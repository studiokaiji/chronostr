import { Layout } from "@/components/layout";
import { Helmet } from "react-helmet";

export const MyPage = () => {
  return (
    <Layout>
      <Helmet>
        <title>chronostr</title>
      </Helmet>
    </Layout>
  );
};
